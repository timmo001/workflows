name: Lint - Pylint

# yamllint disable-line rule:truthy
on:
  workflow_call:
    secrets:
      CODE_PATH:
        required: true

jobs:
  lint-pylint:
    name: 👕 Run Pylint
    runs-on: ubuntu-latest
    steps:
      - name: ⤵️ Check out code from GitHub
        uses: actions/checkout@v3
      - name: 📦 Install Pylint
        run: |
          pip install -U pylint
      - name: 📦 Install
        run: |
          ls -al
          cd ${{ secrets.CODE_PATH }}
          ls -al
          pip install .
      - name: 🚀 Run Pylint
        run: |
          ls -al
          cd ${{ secrets.CODE_PATH }}
          ls -al
          pylint .
